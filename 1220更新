👥 給組員的說明（Google Trace 部分）

除了 PlanetLab 之外，我也完成了 Google Cluster Trace（真實大型資料中心） 的整合，主要用來補足 大規模、異質 PM/VM 的實驗需求，並驗證演算法在「工業級資料中心負載」下的行為。

📊 本次 Google Trace 更新重點
1. 為什麼加入 Google Cluster Trace？

PlanetLab 很適合做：

學術標準對照

CPU-only 的 VM 行為分析

靜態 placement 的 baseline

但它有兩個限制：

沒有真實 PM 容量資訊

規模偏小（~1000 VM）

因此我們加入 Google Cluster Trace (v2011)，補足：

✔ 真實資料中心規模（數萬 machines、數百萬 tasks）

✔ 明確的 PM 容量（CPU / memory）

✔ 真實 VM 的 CPU / memory 使用模式

✔ 業界等級 workload（Borg scheduler）

👉 這讓我們的實驗同時具備
「學術標準（PlanetLab）」+「工業真實性（Google）」

2. Google Trace 資料集來源

資料集：

Google Cluster Trace (2011)

官方釋出、學術界廣泛使用

使用的檔案：

task_usage/part-00000-of-00500.csv.gz

machine_events/machine_events.csv.gz

（只取子集，避免資料量過大）

3. Google Trace 中 VM 與 PM 的定義方式（很重要）
🔹 VM 是什麼？

在 Google Trace 中：

每一個 task ≈ 一個 VM

一個 job（job_id）底下會有多個 task（task_index）

我們將：

(job_id, task_index) → 1 VM

🔹 VM CPU / Memory 是怎麼算的？

來源：task_usage.csv

使用欄位：

usecols = [0,1,2,3,4,5,6,7,10]
names = [
  "start_time",
  "end_time",
  "job_id",
  "task_index",
  "machine_id",
  "cpu_rate",
  "canonical_memory",
  "assigned_memory",
  "max_memory_usage"
]


VM CPU demand：

cpu_demand = max(cpu_rate) × 100


（轉成 0–100 的標準化 CPU 單位）

VM memory demand（重點修正後）：

memory_demand = max(max_memory_usage)


📌 不再使用 canonical_memory（那只是請求值，不是真實用量）

4. PM（Physical Machine）容量怎麼來？

來源：machine_events.csv

Google Trace 中：

每一列代表一台 machine

CPU 與 memory capacity 是「標準化比例值」

我們的轉換方式為：

PM CPU capacity：

cpu_capacity = raw_cpu_capacity × 100


PM memory capacity：

memory_capacity = raw_memory_capacity × 100


這讓 PM 與 VM 都落在 同一個 0–100 的資源空間，可以直接做 placement。

5. 為什麼 Google Trace 也要取樣？

原因和 PlanetLab 類似，但更重要：

原始 Google Trace 規模極大（數十萬 VM）

不取樣：

FFD/BFD 雖然能跑，但時間極長

ILP 完全不可行

多數論文也都「取子集做實驗」

👉 我們的做法是：

固定 random seed

保留 VM / PM 異質性

保留真實 workload 分布

6. Google Trace 實驗定位（和 PlanetLab 的差異）
面向	PlanetLab	Google Trace
資料性質	學術測試平台	真實資料中心
VM 數量	~1000	可達數萬（取樣）
PM 容量	❌ 無	✅ 有
CPU	使用率	使用率
Memory	推估（依分類）	真實使用量
適合用途	baseline / 方法說明	scalability / realism

📌 兩者不是互相取代，而是互補。

7. Google Trace 實驗結果（目前觀察）

以目前的實驗結果來看：

FFD 與 RLS-FFD：

使用較少 PM

CPU utilization 較高

能源消耗較低

BFD：

placement 較分散

active PM 較多

執行時間顯著較長

這與文獻中對 FFD vs BFD 在大規模場景下的行為描述一致。

📁 新增 / 更新檔案（Google Trace）
1. tools/google_trace_to_dataset.py

功能：

讀取 Google Cluster Trace

建立「真實 VM + 真實 PM」資料集

輸出標準 JSON 格式（與 PlanetLab 相容）

2. data/google_dataset/
google_dataset/
├── google_vms_XXXXX.json
└── google_pms_XXXXX.json


可直接丟進現有 placement pipeline。
